runcmd:
  # - DEBIAN_FRONTEND=noninteractive apt-get -y install apt-transport-https ca-certificates gnupg-agent software-properties-common
  # - curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
  # - add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
  # - apt-get update
  # - DEBIAN_FRONTEND=noninteractive apt-get -y install docker-ce docker-ce-cli containerd.io
  # - usermod -G docker ${admin_username}
  # - systemctl enable docker
